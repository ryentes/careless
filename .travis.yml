# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
warnings_are_errors: true
sudo: required

r:
 - release
 - devel
 

r_binary_packages:
  - r-cran-knitr
  - r-cran-testthat
  - r-cran-psych