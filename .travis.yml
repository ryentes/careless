# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

os:
 -linux
 -osx
 -windows

language: R
cache: packages
warnings_are_errors: true
sudo: required

r:
 - release
 - devel
 - oldrel


r_binary_packages:
  - knitr
  - testthat
  - psych
